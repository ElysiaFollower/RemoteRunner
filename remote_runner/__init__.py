"""Target Remote Runner package facade.

The implementation still lives under ``seed_runner`` during the migration. This
package provides the stable target import path without breaking legacy users.
"""

__version__ = "0.1.0"
